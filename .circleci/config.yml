version: 2.1

orbs:
  win: circleci/windows@5.0.0
  
workflows:
  btd:
    jobs:
      - build
jobs:
  build:
    executor:
      name: win/default
      shell: cmd.exe
      
    steps:   
      - run:
          name: Install OpenConnect VPN
          command: choco install openconnect-gui -y
